name: Генерация index.html и index2.html из make_person.py и make_html_copy.cpp

on:
  workflow_dispatch:
  push:
    branches: [main]
  schedule:
    - cron: '0 19 * * *' # ежедневно в 22:00 по Минску (UTC+3)

jobs:
  build-run-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Клонируем репозиторий
      uses: actions/checkout@v3

    - name: 🐍 Устанавливаем Python
      run: sudo apt update && sudo apt install python3 -y

    - name: 🚀 Запускаем make_person.py
      run: python3 make_person.py

    - name: 📂 Проверяем наличие person.txt
      run: |
        if [ ! -f person.txt ]; then
          echo "❌ Файл person.txt не найден!"
          exit 1
        fi

    - name: 🛠 Устанавливаем компилятор C++
      run: sudo apt install g++ -y

    - name: 🧱 Компилируем make_html_copy.cpp
      run: g++ make_html_copy.cpp -o make_html_copy

    - name: 🧾 Запускаем make_html_copy
      run: ./make_html_copy

    - name: 📂 Проверяем наличие index.html
      run: |
        if [ ! -f index.html ]; then
          echo "❌ Файл index.html не найден!"
          exit 1
        fi

    - name: 📂 Проверяем наличие index2.html
      run: |
        if [ ! -f index2.html ]; then
          echo "❌ Файл index2.html не найден!"
          exit 1
        fi

    - name: 📄 Показываем содержимое index.html
      run: cat index.html

    - name: 📄 Показываем содержимое index2.html
      run: cat index2.html

    - name: ⚙️ Настраиваем Git
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"

    - name: 📤 Добавляем и пушим person.txt, index.html и index2.html (если изменились)
      run: |
        git add person.txt index.html index2.html
        if git diff --cached --quiet; then
          echo "🟡 Нет изменений — коммит не требуется"
        else
          git commit -m "✅ Обновлены person.txt, index.html и index2.html"
          git push
        fi
